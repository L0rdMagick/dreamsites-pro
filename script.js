const q=(s,p=document)=>p.querySelector(s),qa=(s,p=document)=>[...p.querySelectorAll(s)];q('#year').textContent=new Date().getFullYear();
const menu=q('.menu'),mobile=q('.mobile-menu');menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));mobile.classList.toggle('open',!open);document.body.classList.toggle('lock',!open)});qa('.mobile-menu a').forEach(a=>a.addEventListener('click',()=>{menu.setAttribute('aria-expanded','false');mobile.classList.remove('open');document.body.classList.remove('lock')}));
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.1});qa('.reveal').forEach(el=>observer.observe(el));
qa('.faq-item button').forEach(btn=>btn.addEventListener('click',()=>{const item=btn.closest('.faq-item'),was=item.classList.contains('open');qa('.faq-item').forEach(x=>{x.classList.remove('open');q('button',x).setAttribute('aria-expanded','false');q('button b',x).textContent='+'});if(!was){item.classList.add('open');btn.setAttribute('aria-expanded','true');q('b',btn).textContent='−'}}));
const launch=q('.chat-launch'),chat=q('.chat'),close=q('.chat header button'),messages=q('.messages'),input=q('.chat form input');function setChat(open){chat.classList.toggle('open',open);chat.setAttribute('aria-hidden',String(!open));launch.setAttribute('aria-expanded',String(open));if(open)setTimeout(()=>input.focus(),200)}launch.addEventListener('click',()=>setChat(!chat.classList.contains('open')));close.addEventListener('click',()=>setChat(false));
const answers={cost:'Every project is different, so we price after a short conversation. You’ll get a clear proposal shaped around what your business actually needs.',time:'Most small-business websites take about 4–8 weeks. A focused landing page can be quicker; a larger site or app may take longer.',ai:'Absolutely. We can add helpful AI chat, appointment scheduling, lead follow-up, and AI phone tools—only where they genuinely make life easier.',start:'The first step is a relaxed consultation. Use the project form below and we’ll find a time to talk about your goals.',other:'That’s a good question. A quick conversation will help us give you an answer that fits your business. Use the project form, or email hello@dreamsites.pro.'};function add(text,user=false){const p=document.createElement('p');p.textContent=text;if(user)p.className='user';messages.appendChild(p);messages.scrollTop=messages.scrollHeight}function reply(text,key){add(text,true);setTimeout(()=>add(answers[key]||answers.other),400)}qa('.replies button').forEach(b=>b.addEventListener('click',()=>reply(b.textContent,b.dataset.key)));q('.chat form').addEventListener('submit',e=>{e.preventDefault();const text=input.value.trim();if(!text)return;const l=text.toLowerCase();let key=/cost|price|much|budget/.test(l)?'cost':/long|time|week|when/.test(l)?'time':/ai|chat|phone|schedul|automat/.test(l)?'ai':/start|begin|call|contact/.test(l)?'start':'other';reply(text,key);input.value=''});
q('#contact-form').addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget),subject=encodeURIComponent('DreamSites project inquiry — '+d.get('service')),body=encodeURIComponent('Name: '+d.get('name')+'\nEmail: '+d.get('email')+'\nService: '+d.get('service')+'\n\nAbout the project:\n'+d.get('message'));q('.form-status').textContent='Opening your email app with the details filled in…';location.href='mailto:hello@dreamsites.pro?subject='+subject+'&body='+body});
